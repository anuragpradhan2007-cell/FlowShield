"""
ML-based Worker Risk Classification Module.

Replaces the hard-coded weighted formula with a trained GradientBoosting classifier
that learns scoring weights and classification boundaries from labeled worker data.
"""
